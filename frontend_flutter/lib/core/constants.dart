class ApiConstants {
  // If using Android Emulator, use 10.0.2.2 instead of localhost
  static const String baseUrl = 'http://localhost:8000/api/v1';
  // Used for prepending the relative paths returned by the server
  static const String serverRoot = 'http://localhost:8000';
}